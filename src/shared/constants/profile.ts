/**
 * The built-in profile ID created by the schema default. Used as a fallback
 * when preferences haven't loaded yet and as the initial profile on fresh
 * installs. Centralised here so every reference stays in sync and a rename
 * only needs a single-file edit.
 */
export const DEFAULT_PROFILE_ID = "default";
